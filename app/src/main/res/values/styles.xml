<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyDefaultTextAppearance" parent="@android:style/TextAppearance">
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textStyle">bold</item>
        <item name="android:padding">@dimen/_3dp</item>

    </style>

    <style name="AppTheme.Card.Margins" parent="CardView">
        <!--<item name="android:layout_marginTop">@dimen/_0dp</item>-->
        <!--<item name="android:layout_marginBottom">@dimen/_5dp</item>-->
        <!--<item name="android:layout_marginLeft">@dimen/_0dp</item>-->
        <!--<item name="android:layout_marginRight">@dimen/_0dp</item>-->
        <item name="cardElevation">1dp</item>
        <item name="cardUseCompatPadding">true</item>
        <item name="android:foreground">?android:attr/selectableItemBackground</item>
        <item name="cardCornerRadius">@dimen/_0dp</item>
        <!--<item name="cardPreventCornerOverlap">false</item>-->
        <item name="cardBackgroundColor">@android:color/white</item>
    </style>
</resources>
